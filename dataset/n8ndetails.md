| 컬럼명                    | 한글명(의미)      |                            타입/예시 |  필수 | 설명/규칙                                          |
| ---------------------- | ------------ | -------------------------------: | :-: | ---------------------------------------------- |
| `batch_file_id`        | 배치 파일 ID     |        STRING / `BF20260113-001` |  Y  | 배치 파일을 식별하는 ID. 파일 단위 추적/로그에 사용                |
| `batch_date`           | 배치 기준일       |              DATE / `2026-01-13` |  Y  | “전날 승인 거래 확정분” 같은 배치 기준 날짜                     |
| `source_system`        | 생성 주체(출처)    |                 STRING / `VAN01` |  Y  | 배치 생성/전송 시스템 식별(예: VAN/PG 코드)                  |
| `customer_key`         | 고객 키(통합 식별자) |                 STRING / `C0001` |  Y  | 통합 배치 내 사용자 구분 키. 실무에선 고객번호/회원ID/카드토큰 등일 수 있음  |
| `card_token`           | 카드 토큰(가명처리)  |          STRING / `TKN_C0001_01` |  N  | 카드번호를 대체하는 토큰 값(가명처리). 분석/식별에 쓰되 원문 PAN 저장은 지양 |
| `txn_datetime`         | 거래(승인) 일시    | DATETIME / `2026-01-13 08:12:34` |  Y  | 승인 발생 시각. 타임존: Asia/Seoul 가정                   |
| `approval_no`          | 승인번호         |               STRING / `A839201` |  Y  | 승인 거래의 고유 식별자 역할. 취소/정정 매칭에도 사용 가능             |
| `merchant_name`        | 가맹점명         |              STRING / `스타벅스 강남점` |  Y  | 사용자에게 보이는 가맹점 이름(정제 대상: 공백/특수문자 등)             |
| `merchant_id`          | 가맹점 ID       |               STRING / `M000101` |  Y  | 시스템 내부 가맹점 식별자(없으면 merchant_name로 대체 가능)       |
| `mcc`                  | MCC 코드       |              STRING/INT / `5814` |  N  | 카드업종분류코드(가맹점 업종). 카테고리 분류 근거                   |
| `category`             | 카테고리(업종명)    |                    STRING / `카페` |  N  | 사람이 이해하기 쉬운 업종/카테고리 라벨(예: 교통, 배달, 구독 등)        |
| `amount`               | 거래 금액        |                     INT / `5800` |  Y  | 결제 금액. 취소건도 금액은 동일하게 들어오고 `is_cancel`로 구분      |
| `currency`             | 통화           |                   STRING / `KRW` |  Y  | 통화 코드(기본 KRW)                                  |
| `is_cancel`            | 취소 여부        |                BOOLEAN / `false` |  Y  | `true`면 취소(또는 부분취소/정정) 거래. 기본값 `false`         |
| `original_approval_no` | 원거래 승인번호     |               STRING / `A839207` |  N  | 취소/정정 시 원 승인건을 가리키는 값. `is_cancel=true`일 때 채워짐 |
| `installment_months`   | 할부 개월        |                   INT / `0`, `2` |  Y  | 0이면 일시불, 2 이상이면 할부 개월수                         |
| `channel`              | 채널           |       ENUM / `OFFLINE`, `ONLINE` |  Y  | 오프라인 단말 결제 vs 온라인 결제 구분                        |
| `city`                 | 지역(시/도)      |                    STRING / `서울` |  N  | 거래 발생 지역(샘플에서는 단순화). 실무에선 시군구/국가까지 존재 가능       |
